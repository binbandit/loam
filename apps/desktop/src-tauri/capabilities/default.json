{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege grants for the main window (§5.10). No filesystem, no shell, no process execution, no generic command bridge — vault-scoped filesystem access arrives only through typed E06 IPC commands enforced in loam-core.",
  "windows": ["main"],
  "platforms": ["macOS", "windows", "linux"],
  "permissions": ["core:app:default", "core:event:default", "core:window:default"]
}
