// Negative fixture: intentionally unformatted so `rustfmt --check` must fail.
// Not part of any workspace crate. Verified by `pnpm gates:check`.
fn main(){let greeting="hello";println!("{greeting}");}
