// Sample component proving the Vitest + Testing Library harness (LOA-8).
// Replaced by the real design-system primitives in E07.
export function Hello({ name }: { name: string }) {
  return <p>Hello {name}</p>;
}
